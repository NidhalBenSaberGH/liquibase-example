server.port=${PORT:5001}
#=============================={ spring datasource }==================================
spring.datasource.url = jdbc:postgresql://localhost:5432/demoliquibase
spring.datasource.username = byblos
spring.datasource.password = byblos
spring.datasource.driverClassName=org.postgresql.Driver

spring.jpa.generate-ddl=false
spring.jpa.hibernate.ddl-auto = none

#spring.datasource.driver=org.h2.Driver
#spring.datasource.url=jdbc:h2:mem:activiti-admin;MODE=MYSQL;DB_CLOSE_DELAY=1000
#spring.datasource.username=sa
#spring.datasource.password=
#spring.datasource.driver-class-name=org.h2.Driver

#hibernate.dialect=org.hibernate.dialect.H2Dialect
hibernate.show_sql=false

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
#spring.jpa.hibernate.ddl-auto =  create-drop
#checkingQuery= select 1

#spring.liquibase.enabled=false


